import { useState } from "react";
import { emptyList } from "./constants";

function App() {
  const [taskList, setTaskList] = useState([]);

  return (
    <div>
      extra
    </div>
  );
}

export default App;
